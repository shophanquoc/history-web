export default function Stats() {
    return ( <
        div style = {
            { padding: 20 } } >
        <
        div className = "card"
        style = {
            { textAlign: "center" } } >

        <
        div style = {
            { fontSize: 30 } } > ⏱️ < /div>

        <
        h2 > 48 + < /h2> <
        p > Năm lịch sử < /p>

        <
        /div> <
        /div>
    );
}